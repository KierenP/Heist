//#ifndef DRAWABLEENTITY_H
//#define DRAWABLEENTITY_H

/*
    15/7: Disregard this class, it is currently a work in progress, and now after reaseaching what I aim to achieve needs major refactoring.
*/

/*
class DrawableEntity
{
public:
    DrawableEntity();
    ~DrawableEntity();

    float GetPosX() { return PosX; }
    float GetPosY() { return PosY; }
    //sf::Drawable* GetRenderObj() { return RenderObj; }    Commented out till SFML is linked correctly

    void SetPosX(float val) { PosX = val; }
    void SetPosY(float val) { PosY = val; }
    //void SetRenderObj(sf::Drawable* val);                 Commented out till SFML is linked correctly

    //void Render(sf::RenderWindow* pTarget);                Commented out till SFML is linked correctly

private:
    float PosX;
    float PosY;
    //sf::Drawable* RenderObj;                              Commented out till SFML is linked correctly
};

#endif // DRAWABLEENTITY_H*/
